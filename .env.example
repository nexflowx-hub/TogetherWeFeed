# Together We Feed — environment variables
# Copy to .env.local / configure in Vercel. Never commit live credentials.

# XPayments Native S2S API (PIX)
XPAYMENTS_API_BASE_URL=https://api.xpayments.digital/api/v1

# Native Store/API keys.
# BRL is the PIX/MisticPay Store and must remain isolated from Stripe Direct.
XPAYMENTS_API_KEY_BRL=
# EUR is retained for backwards compatibility and may also be used as the
# Stripe Direct key while TWF-EUR remains a Stripe-compatible Store.
XPAYMENTS_API_KEY_EUR=

# XPayments Stripe-compatible Direct
XPAYMENTS_STRIPE_BASE_URL=https://api.xpayments.digital/api/stripe/v1

# Dedicated Store keys for Stripe Direct. EUR falls back to XPAYMENTS_API_KEY_EUR
# during migration; BRL deliberately requires a separate Stripe-compatible Store.
XPAYMENTS_STRIPE_API_KEY_EUR=
XPAYMENTS_STRIPE_API_KEY_BRL=

# Browser-safe Stripe publishable keys belonging to the Stripe account routed
# by the corresponding XPayments Stripe Direct Store. These are returned by the
# server to Stripe.js; do not put sk_* / rk_* credentials here.
XPAYMENTS_STRIPE_PUBLISHABLE_KEY_EUR=
XPAYMENTS_STRIPE_PUBLISHABLE_KEY_BRL=

# Merchant webhooks are optional for payment initiation. Configure later when
# Together We Feed needs authoritative asynchronous donation finality.
XPAYMENTS_WEBHOOK_SECRET_EUR=
XPAYMENTS_WEBHOOK_SECRET_BRL=

# Public application URL
NEXT_PUBLIC_SITE_URL=https://example.org
